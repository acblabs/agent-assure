variant_id: expense-baseline
pipeline_id: expense-approval-baseline
runner_id: expense_approval.minimal
provider_policy:
  forbidden_providers:
    - unapproved-expense-model
  runtime_allowed_providers:
    - unapproved-expense-model
behavior:
  evidence_assembly: association_preserving
  provider_policy_precedence: policy_over_runtime
