# Experiment Protocol

The current protocol scope is offline artifact validation. Live stochastic
experiments require a later protocol covering hypotheses, sample sizes,
confidence intervals, retry rules, provider-version capture, rate limits, cost
budgets, and exclusion rules.
