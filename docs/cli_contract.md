# CLI Contract

Current commands:

- `agent-assure --help`
- `agent-assure validate PATH --kind KIND`
- `agent-assure schema export --out DIR`
- `agent-assure suite lint PATH`
- `agent-assure suite compile PATH --out PATH`
- `agent-assure otel preview PATH [--out PATH]`

Later command groups exist as placeholders and do not claim completed behavior.
