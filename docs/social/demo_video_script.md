# Demo Video Script

1. Install package.
2. Run one-command demo.
3. Show baseline approve.
4. Show candidate approve.
5. Show decision fields preserved.
6. Show missing evidence link.
7. Show CI gate blocked.
8. Open `evidence-diff.html`.
9. Close with: matching decision fields are not process equivalence.

Suggested narration:

```text
Here is the shortest version of agent-assure. Install it, run the flagship demo,
and inspect the local artifacts. The visible answer stays the same on both
sides: approve and approve. The candidate still fails because it drops the
material claim-duration evidence link. That is the point: matching decision
fields are not process equivalence.
```
